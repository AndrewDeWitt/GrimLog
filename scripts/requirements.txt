# Python dependencies for content scraping and transcript fetching
# Install with: pip install -r scripts/requirements.txt

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0
yt-dlp>=2024.1.0

# Web scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0  # Optional but faster HTML parser for BeautifulSoup

# Note: OpenAI Whisper and Gemini are accessed via HTTP API, no Python packages needed

